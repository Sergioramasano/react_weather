import "./Heading.scss";
export const Heading = (props) => {
  return (
    <div id="container">
      <h1>{props.name}</h1>
      <h1>{props.name}</h1>
      <h1>{props.name}</h1>
    </div>
  );
};
